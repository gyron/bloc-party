import { Bloc } from './bloc'
import { Config } from './config'

export class BlocParty {

	constructor(private config: Config) {
		// loop over the dom nodes and create the

		let bloc = new Bloc()
		bloc.test()
	}
}